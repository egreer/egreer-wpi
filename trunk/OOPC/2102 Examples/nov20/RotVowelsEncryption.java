package nov20;

/**
 * Rotate vowels (A -> E, E -> I, I -> O, O -> U, U -> A)
 * 
 * @author heineman
 */
public class RotVowelsEncryption extends Encryption {

	@Override
	String encrypt(String plainText) {
		String enc = "";
		for (int i = 0; i < plainText.length(); i++) {
			
		}
		
		return enc;
	}

}
