/*
public class test {
	public static void main(String args[]){
		String S = "";
		int q = S.length();
		int[] A; 
		A = new int[q,q,3];
		
		for (int i= 0; i < S.length(); i++){
			for (int j = 0; j < S.length(); j++){
				for(int r = 0; r< j-i; r++ ){
					char t = MULTIPLY( MULTIPLY(S.substring(r,j)) , MULTIPLY(S.substring(i, r)))
					
					if (t == 'a'){
						A[i,j,a] = r;
					
					}else if (t == 'b'){
						A[i,j,b] = r;
						
					}else{
						A[i,j,c] = r;
					}
				}
				
			}
		}
		
		return A[1,n, A];
		
	}
}*/
