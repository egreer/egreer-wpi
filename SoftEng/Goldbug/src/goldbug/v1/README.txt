Better.

We have extracted much of the logic from the program and placed it in
a separate class, called "Code". This represents a code fragment with
potential (key,value) decoding pairs.

Now we need to find some way to break-up the interaction loop in the program.
